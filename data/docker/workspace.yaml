load_from:
  # Each entry here corresponds to a service in the docker-compose file that exposes user code.
  - grpc_server:
      host: dagster-footballpace-data
      port: 4000
      location_name: "dagster-footballpace-data"
